export const RUNNER_CAPABILITIES = Object.freeze({
  claude: Object.freeze({
    id: 'claude',
    name: 'Claude Code',
    command: 'claude',
    supports: Object.freeze({
      model: true,
      defaultModel: 'sonnet',
      effort: ['default', 'low', 'medium', 'high', 'xhigh', 'max'],
      permissionMode: ['default', 'acceptEdits', 'auto', 'bypassPermissions', 'dontAsk', 'plan'],
      resume: true,
      sessionId: true,
      browser: ['none', 'repository', 'chrome', 'mcp'],
      outputFormat: ['text', 'json', 'stream-json'],
      configFields: ['model', 'effort', 'permissionMode', 'outputFormat', 'timeoutMs', 'maxBudgetUsd', 'gatewayUrl', 'apiKeyKind', 'apiKey', 'browserMcpConfigsJson'],
      defaultEffort: 'high',
      defaultOutputFormat: 'json',
      defaultApiKeyKind: 'auth_token',
    }),
  }),
  codex: Object.freeze({
    id: 'codex',
    name: 'Codex CLI',
    command: 'codex',
    supports: Object.freeze({
      model: true,
      defaultModel: '',
      effort: ['default', 'none', 'minimal', 'low', 'medium', 'high', 'xhigh', 'max', 'ultra'],
      permissionMode: ['default', 'auto', 'bypassPermissions', 'plan'],
      resume: false,
      sessionId: false,
      browser: ['none'],
      outputFormat: ['json', 'stream-json'],
      configFields: ['model', 'effort', 'permissionMode', 'timeoutMs'],
      defaultEffort: 'default',
      defaultOutputFormat: 'stream-json',
    }),
  }),
  grok: Object.freeze({
    id: 'grok',
    name: 'Grok CLI',
    command: 'grok',
    supports: Object.freeze({
      model: true,
      defaultModel: '',
      effort: ['default', 'low', 'medium', 'high', 'xhigh', 'max'],
      permissionMode: ['default', 'acceptEdits', 'auto', 'bypassPermissions', 'dontAsk', 'plan'],
      resume: true,
      sessionId: true,
      browser: ['none'],
      outputFormat: ['text', 'json', 'stream-json'],
      configFields: ['model', 'effort', 'permissionMode', 'outputFormat', 'timeoutMs', 'gatewayUrl', 'apiKey'],
      defaultEffort: 'high',
      defaultOutputFormat: 'stream-json',
      defaultApiKeyKind: 'api_key',
    }),
  }),
  agy: Object.freeze({
    id: 'agy',
    name: 'Antigravity CLI',
    command: 'agy',
    supports: Object.freeze({
      model: true,
      defaultModel: '',
      effort: ['default', 'low', 'medium', 'high'],
      permissionMode: ['default', 'acceptEdits', 'auto', 'bypassPermissions', 'plan'],
      resume: true,
      sessionId: false,
      browser: ['none'],
      outputFormat: ['text'],
      configFields: ['model', 'effort', 'permissionMode', 'timeoutMs', 'gatewayUrl', 'apiKey'],
      configOptions: Object.freeze({
        permissionMode: ['acceptEdits', 'bypassPermissions', 'plan'],
      }),
      defaultEffort: 'medium',
      defaultOutputFormat: 'text',
      defaultApiKeyKind: 'api_key',
    }),
  }),
});
